# Notes on the AI-generated comparison draft

**What this file is.** A record of how `AI-GENERATED-DRAFT-do-not-circulate.tex`
was written: every judgement call, every place the source document could not
supply something, the word count, and the internal inconsistencies found in
`docs/paper-source.md` while writing from it.

**Sources used.** `docs/paper-source.md` and `docs/paper-source.json` only.
`docs/findings.md`, `docs/literature-corrections.md`, the figure scripts and the
code were not opened for this draft. Figure files were referenced by the paths
given in the source document's figure inventory (§10); their contents were not
inspected, and no caption was rewritten beyond tense and cross-reference.

**Nothing was computed.** Every number in the draft is copied from the numbers
table (§13), the ledger (§5) or the claims register (§7) of the source document.
No arithmetic was performed on them, including no unit conversions, no
re-rounding and no derived percentages.

---

## 1. Word count

| region | words |
|---|---|
| Introduction through the closing Disclosure, excluding figures, tables, appendices and references | **10,854** |
| the same, plus the mandated Section 1 Disclosure | 11,030 |
| abstract (separate, not counted above) | 293 |

The article body is inside the 8,000–11,000 target. Counting the mandated
disclosure furniture in Section 1 as body text puts it 30 words over, which is
inside the noise of the counting method described below. The count is by a script that strips
LaTeX markup, keeps the text inside `\textbf`/`\emph`, drops figure and table
environments, and counts a token as a word if it contains a letter or digit;
quantities like `96.9%` count as one word.

**Compilation.** `pdflatex`, three passes, **succeeded**: 38 pages, no errors,
no undefined references, no undefined citations, no overfull boxes. One overfull
`\hbox` in the terrain parameter table was found and fixed by splitting a cell.

**Revision.** This draft was revised once after review by the repository owner.
Section 8 below lists what the review found and what changed; every hard error it
identified was in the draft rather than in the source document.

**Rebuilding.** The compiled PDF is a build product and is not committed, in
keeping with the repository's rule for generated artefacts. To rebuild:
regenerate `figures/` from the configs and figure scripts named in the source
document's inventory (§10), then run `pdflatex` three times on
`paper/AI-GENERATED-DRAFT-do-not-circulate.tex`. The file name carries the
disclosure marker, so the PDF does too.

**Class.** `sn-jnl.cls` (Springer Nature journal class, the Swarm Intelligence
template) **is not installed in this environment**, so the draft uses `article`
with the same structure, as the instruction permits. Section order, appendix
structure and the reference style are unchanged by this; a Springer submission
would need the class swapped and the bibliography restyled.

---

## 2. Disclosure markers (all mandatory, all present)

Verified in the compiled PDF by decompressing its content streams and matching
kerning-split text:

| marker | where | occurrences in the PDF |
|---|---|---|
| `AI-GENERATED DRAFT` (watermark, diagonal, grey) | every page | 38 |
| running header `AI-GENERATED DRAFT — NOT FOR CIRCULATION` | every page | 38 |
| footer `Generated by Claude (Anthropic) as a comparison baseline. Not human-authored. Do not circulate.` | every page | 38 |
| bracketed title prefix, own line | title page | 1 |
| author block, verbatim, no human name anywhere | title page | 1 |
| abstract first sentence, verbatim | abstract | 1 |
| `[AI-generated draft]` caption prefix | all 12 figures and all 5 tables | 17 captions (21 occurrences in all, the rest being in-text references to the marker) |
| boxed Disclosure paragraph | Section 1 | 1 |
| closing Disclosure paragraph | after the Conclusion | 1 |
| source comment block | top of the `.tex` | 1 |

The caption prefix is applied by `\DeclareCaptionLabelFormat{aidraft}` rather
than typed per caption, so it cannot be lost by editing a caption. The filename
carries the marker, and so does the PDF's.

---

## 3. `[SOURCE GAP]` entries — 8, all in the bibliography

Every gap is the same kind: the source document's literature table (§11) records
the authors, the venue and the year for these references but **not the title**,
and the instruction forbids inventing one. Each is marked in place rather than
filled, and no gap affects a claim — every one of these references is cited for
context, none carries a number.

1. Birattari et al. (2019) — title of the automatic-design manifesto not recorded.
2. Francesca et al. (2014) — title not recorded (the AutoMoDe work).
3. Hunt (2020) — title of the phenotypic-plasticity position paper not recorded.
4. Jakobi, Husbands and Harvey (1995) — title not recorded (minimal simulations / reality gap).
5. Johnson and Brown (2015) — title not recorded (perimeter, rendezvous, foraging).
6. Ligot and Birattari (2018) — title not recorded.
7. Olson, Hintze, Dyer, Knoester and Adami (2013) — title not recorded. **This is
   the one worth fixing first**: it is the source of the confusion mechanism the
   pursuer model instantiates, and it is cited in the Methods for the functional
   form of `p_lock`.
8. Schmickl, Kernbach et al. (2008) — title not recorded (BEECLUST).

Initials were also **not** invented: entries give surnames as the source document
records them. Volume, issue and page numbers are absent throughout for the same
reason and are not marked individually, since no entry in the source document
has them.

## 4. `[CITATION NEEDED]` entries — 4 instances, 3 distinct topics

These are the three the source document itself flags as having no bibliographic
entry anywhere in the repository:

1. **Holling's disc equation, original reference** — cited in Methods for
   handling time. One instance.
2. **Graham & Sloane, normalised second moment** — the convention the dispersion
   metric follows. Two instances (Methods, where the metric is defined, and
   Limitations, where the provisional normalisation is discussed).
3. **Specific insider-adversary references** — cited in Related work only to
   distinguish a different threat model. One instance.

---

## 5. Judgement calls

### 5.1 What was emphasised

* **The methodology results (Results C) were given equal weight to the two
  environment results**, and the seed-scatter result was made the section's
  closing subsection rather than a footnote. The source document grades the
  three-seed finding SUPPORTED (S22) with a pre-registered rule that fired, and
  it changes what a reader may quote from every other searched row. Burying it
  would have made the class-search result look cleaner than the record allows.
* **The 96.9% / 3.1% decomposition was put in the abstract, the Discussion and
  the Conclusion.** It is the finding most likely to be mis-taken from the raw
  hold ratios, and the source document's correction #11 exists precisely because
  the project itself took it wrongly at first.
* **The dispersive control was foregrounded in Results B** ahead of the survival
  numbers, because it is what licenses the causal reading of the confusion
  effect; the numbers alone would only show a correlation with strategy.

### 5.2 What was cut

* **The occlusion axis is Methods-only.** The source document records exactly one
  shakedown sweep, no capability row searched under it, and grades the finding as
  a methods warning (S20). It appears in the Methods and in the Limitations, and
  the two occlusion figures were **not** included: the draft makes no occlusion
  claim, so a figure for one would over-signal.
* **`terrain_h2_r0_scaling.png` was excluded** — the inventory marks it
  superseded. **`terrain_h2_powered.png` was excluded** although marked
  paper-ready-with-a-caveat, because the ratio-law reading it most naturally
  supports is exactly the reading the paper reports as ruled out (N6); its
  surviving result (R₀ and not the axle) is stated in the text instead.
* **`terrain_lambda_sweep.png` was excluded** as subsumed by
  `terrain_lambda_r0_family.png`, which covers a wider λ range and carries the
  validity window. **`terrain_retune_cost.png`, `terrain_warm_s4.png`,
  `terrain_mechanism_regression.png`, `terrain_decision_rule_regimes.png`,
  `pursuer_dispersive_surface.png`, `pursuer_idea_b_surface.png` and
  `gauci_scaling.png` were excluded** for length: each supports a claim already
  carried by an included figure or by numbers in the text. All are paper-ready
  and any of them could be restored. **12 of the inventory's 26 figures are
  included**, `terrain_lambda_r0_family.png` among them.
* **The n = 3 / n = 4 reach dip, the scalar field's ~2% improvement and the
  anisotropy lemma appear only in the Limitations**, not in Results. The register
  grades the first two SUGGESTED-with-hedge or "recorded, not claimed", and the
  third does not exist.
* **The full ledger (source §5, 21 entries) was not reproduced.** Its content is
  distributed across Methods, Results and the appendices; a 21-row ledger in the
  paper would duplicate the appendices without adding a claim.

### 5.3 What was hedged, and how

Every SUGGESTED claim is stated in the register's own wording, or in a
paraphrase that keeps all of its qualifiers. Specifically:

* **G1 (body diameter)** — kept the whole hedge: two usable body sizes, the third
  row's 72% reach, the slope dropping to 0.474 if it is included, the three
  unseparated body-scaled lengths, and "no mechanism is proposed".
* **G2 (fixed vs sub-proportional)** — the draft says proportionality is rejected
  and a weak sub-proportional dependence is not excluded, with the grid
  resolution stated in the same breath. It never says the length scale is fixed.
* **G4 (the pursuer sensor state)** — stated as a lower bound among hand-designed
  rows, with the five-state row's poor showing labelled a hand-design artefact.
  The Discussion repeats the hedge rather than relying on the Results section
  having stated it.
* **S11 and S12 (the pursuer range claims)** — every statement of either now
  carries the κ it holds at. In the first draft the Conclusion did not, and said
  something false as a result; see §8.
* **G5 (R₀ and gathering rate)** — stated as an association across five
  controllers, with the explicit note that R₀ was never swept as a dial.
* **G10 (the axle as a spatial gradient sensor)** — used only as an observation,
  with the ten-to-one ratio of the mean-traction term to the gradient term and
  the absence of any temporal-versus-spatial experiment both stated. The
  Berg & Purcell connection is made in one sentence and immediately bounded.
* **No grade was raised anywhere.** The twelve NOT SUPPORTED readings appear only
  in Appendix C and, where a reader might otherwise arrive at one (N2, N4, N5,
  N6, N7), as an explicit "this is what the data does not say" in the Results.

### 5.4 The hold-ratio definition (source §12.1, D1)

The source document flags two statistics both called "hold ratio" and instructs
the paper to use **one**. The draft uses the **ratio-of-medians** values
(2.208 / 1.195 / 1.104) throughout, because the claims register states S4 and S5
in those numbers and the draft may not recompute. The paired-ratio values
(2.149 / 1.200 / 1.099) are **not** quoted anywhere, so the same 100 runs are
never presented twice as different cells. The one place a paired ratio appears is
the class-search cell at τ = 3600 s (1.436), which the source document reports
only in that form and which is a different experiment.

The source document's own preference is the paired ratio. **A human author who
prefers it must restate S4, S5 and every §13-derived number in the draft, not
just swap the three headline values.** This is flagged here rather than acted on
because acting on it would require recomputation.

### 5.5 Other calls

* **S1's condition was restated as "λ = 0.10 m" rather than "λ/R₀(gauci) = 0.69".**
  The register phrases S1 with the ratio; N6 rules out reading that ratio as a
  law. Keeping the ratio phrasing in the Results would have implied the law two
  subsections before the paper rules it out. The condition is identical.
* **"Reach" and "hold" are defined once in Methods and separated everywhere**,
  per S18, including in the seed-study cells where reach and dispersion disagree.
* **The abstract states "All results are simulation only"** as its last sentence,
  rather than leaving it to the Limitations.
* **The follow-up list in the Conclusion is drawn from source §12.4**, with the
  four items the freeze names as not-to-be-run listed first and the rest after.
  Items §12.4 marks as answered are not listed as open.
* **`R₀ = 4.7 cm` vs `4.74 cm` (D2) and `L2 = 0.516` vs `0.515` (D3)** — the
  draft uses 4.74 cm, which the source document calls exact, and does not quote
  the L2 distance in the body at all.

---

## 6. Internal inconsistencies found in `docs/paper-source.md`

Four, none fatal; the first two changed how the draft is worded. **This section
lists inconsistencies in the *source document* only. Seven were later found in
the draft itself, by review rather than by any check run here — see §8, which
also supersedes item 1 below.**

1. **S11 and S12 read as contradictory at the same cell.** S11: "Beyond
   r_p ≈ 0.47 R the dispersive row survives far better." S12: "At r_p = 0.47 R,
   κ = 3 the dispersive row is Pareto-dominated: every aggregating row beats it on
   both survival and the base task." Both are at the same pursuer range, and one
   says the dispersive row survives far better while the other says every
   aggregating row beats it on survival. **They are reconcilable through κ** —
   number 46 gives survival at that cell as 0.00 / 0.55 / 0.65 for the aggregating
   baseline, the hand-designed row and the dispersive row, which is a κ-swept
   figure, while S12's cell is specifically κ = 3 — but the register does not say
   so, and a reader taking the two claims at face value would find them
   incompatible. **Superseded by §8.1(2):** the κ-dependence is not a caveat to be
   attached, it is the result, and S11 and S12 in Appendix C have been restated
   accordingly.
2. **The claims register states S1 with a ratio (λ/R₀ = 0.69) that N6 rules out
   as a law.** Handled as in §5.5 above. Not an error — S1 is naming a condition,
   not asserting a law — but the phrasing invites the reading N6 exists to
   prevent.
3. **Limitation L20's row is malformed markdown**: the status cell opens with
   `****closed for the flat class by §21**` and the emphasis never closes
   cleanly, so the row renders with stray asterisks. Content is legible;
   formatting only.
4. **The freeze is recorded as two commits** — "`ce427a8` (experimental record) /
   `2b736de` (freeze declaration)" in §12.5, against a single "freeze commit hash"
   elsewhere in the same document. The draft does not quote a commit hash, so
   nothing depends on it, but the two-hash form should be made consistent.

Two further things are *stated* inconsistencies rather than found ones — the
source document flags them itself and the draft follows its ruling: D1 (the two
hold-ratio definitions, §5.4 above) and D2/D3 (rounding at the last digit).

---

## 7. What a human author should check first

1. The Olson et al. (2013) reference (gap 7) — it is load-bearing for the pursuer
   model and currently has no title.
2. The hold-ratio definition (§5.4) — a one-line decision with paper-wide
   consequences.
3. Whether any of the eight excluded paper-ready figures should be restored; the
   draft is at the top of its word budget, so restoring them means cutting text.
4. Whether Results C should be a separate paper (§8.3).

---

## 8. Review pass, and what it changed

The repository owner reviewed the first draft. Every hard error it found was in
**the draft**, not in the source document — which is the honest verdict on §6
above: the consistency check that produced §6 was run against
`docs/paper-source.md` and never against the output. That is the process defect
worth fixing before the next draft, and it is why §8.1 exists.

### 8.1 Hard errors found and fixed

1. **A sign contradiction on the flat-ground effect.** Results A said the
   terrain-tuning term reverses sign on flat ground and costs 4.1%; the
   Discussion said the reverse — that the *rough*-trained row is the better
   flat-ground controller. The source is unambiguous ("S2-rough costs 4.1%
   against S2-flat"), the hold ratios back it out the same way (1.438/1.195 =
   1.203 against 1.383/1.104 = 1.253, lower being better), and so does the
   paired-difference panel. **Results A was right; the Discussion sentence was
   wrong and has been replaced**, and it was the paragraph a reviewer would
   quote. This was a generation error with no counterpart in the source.

2. **The Conclusion dropped κ from the pursuer result and contradicted itself.**
   It read "Pareto-dominant at short pursuer range and catastrophic beyond
   roughly half the swarm radius" — but the Pareto-dominant cell *is* at 0.47 R,
   roughly half the swarm radius, and holds only at κ = 3. The replacement states
   the mechanism instead: the dispersive row is nearly κ-insensitive (1.20/1.13)
   while the aggregating rows swing 3.05–4.10, **so the ordering at a given range
   is a function of κ, not of range alone**. The abstract had the same defect and
   now carries the same statement.

   **This also supersedes §6.1's account of the S11/S12 tension.** The earlier
   note said the two claims are "reconcilable through κ" and left it there. The
   correct resolution is stronger: the κ-dependence is not a caveat attached to
   two claims, it is the finding, and it follows directly from the control the
   experiment was built around. S11 in Appendix C has been restated with its κ
   and its pooling; S12 now reads "at 0.47 R *and* κ = 3".

3. **The headline pursuer comparison was confounded.** "Aggregation is on average
   a net liability" rested on the blind row at 0.00 against the dispersive row at
   0.65 — but the dispersive row has pursuer sensing and a flee response and the
   blind row has neither, so that pair varies capability and spatial strategy at
   once. The matched pair is the **ternary** row against the dispersive row, both
   hand-designed, both with identical pursuer sensing. Results B now leads with
   the matched pair, states the full survival table for all three rows, and
   reports what the matched pair actually shows: aggregating wins the two
   shortest ranges, the intervals overlap at 0.47 R, dispersing wins past it, so
   **the crossover lies between 0.47 R and 0.81 R** and the paper cannot place it
   more finely. This was the weakest inference in the draft and the fix makes the
   claim narrower and better supported.

4. **"Close to the nominal" for a realised rate of 0.89 against a nominal 0.6.**
   The phrasing undercut its own finding and has been replaced with "can run far
   above the nominal one".

5. **An unexplained handling time.** Table 3 listed h = 5.0 s alongside the two
   values the body justifies. It turns out 5.0 s is not orphaned — it is the
   handling time of the *first* pursuer sweep, whose survival medians are the
   ones quoted in Results B's capability subsection. The body never said so.
   Methods and the table now name it, and the same sentence resolves the
   "pooled" ambiguity below.

6. **Steinberg and Solovey's year.** Cited as (2024) against a reference giving
   arXiv:2501.00390 and "IEEE, 2025". **2025 is now used in all four places**,
   and the bibliography entry records that the project's own literature record
   dates the preprint 2024 while the arXiv identifier and the IEEE entry both say
   2025. A human author should settle which the repository means.

7. **An orphan number.** S16 in the claims register quoted a training objective
   of 1.2595 that appeared nowhere else in the draft. It is the warm-start
   search's reported objective, and it now appears where the warm start is
   described, alongside the two cold searches' 1.3012 and 1.3009 — which makes it
   the first of the paper's three encounters with an over-optimistic training
   score rather than a loose number in an appendix.

### 8.2 Structural problems, and what was done

* **The Framework promised apparatus the paper never uses.** It defined threshold
  contours P = T and a frontier read off the lowest enclosing contour, then the
  Limitations admitted no T was ever pre-registered and no across-row test was
  ever run. **The threshold-contour machinery has been cut** and replaced with an
  explicit "no frontier table is produced", naming both deviations at the point
  the reader would otherwise form the expectation. The capability vector and the
  two provenance marks — which the paper does use, consistently — stay.
* **Two of the vector's four coordinates never vary.** The Framework now says so
  outright: A is 0 in every row, K was never run, and the vector is an accounting
  unit rather than a space this paper searches.
* **The strongest negative result was stranded in an appendix.** S21 (the
  noise-helps null: three independent tests, 200 runs/cell, 0 of 23 cells
  disjoint from baseline) was graded SUPPORTED, forward-referenced from Related
  work, and then never delivered. **Results A now opens with it** as
  "Small perturbations do not help", with the fine sweep's numbers and the reason
  the discrete-model analogy fails. The forward reference now resolves.
* **Results A switched between hold ratios and absolute dispersion silently.**
  One sentence now says which is which and that a 1.10 hold ratio and a 1.383
  absolute dispersion describe the same row.
* **"Pooled" did undefined work.** The blind row's 0.00 in three columns and its
  0.350 median over the grid come from different sweeps at different handling
  times under different poolings. Results B now says so explicitly.

### 8.3 What was *not* done, and why

**Results C was not moved.** The review is right that it is a self-contained
methods contribution, that it shares almost no machinery with the two
experimental sections, and that it is the part most likely to be cited from the
bottom of a 38-page paper. But reordering it ahead of Results A and B would
invert the paper's organising claim — the passive/active hostility contrast that
the Discussion is built on — and that is a decision about what the paper *is*,
which belongs to the author rather than to a comparison draft. Two smaller things
were done instead: Results C now opens by saying it can be read on its own, and
the Introduction's third contribution says it stands independently of the other
two. **If the author wants it as its own paper, nothing in Results C depends on
Results A or B beyond the simulator and the anchor controller.**

### 8.4 Cuts made to stay inside the word budget

The fixes above added roughly 900 words. To stay inside 8,000–11,000 the
following were tightened, none of them a claim: Methods (the exact-arc and
contact-model detail, the five wrong hypotheses behind the reproduction gate, the
search-protocol prose), Related work (BEECLUST and confusion paragraphs), the
Framework, the Conclusion's restatement of results the Discussion already makes,
the Discussion's three implication paragraphs, and the Introduction's
contributions list. **No number, hedge, grade or limitation was dropped** to make
room.
